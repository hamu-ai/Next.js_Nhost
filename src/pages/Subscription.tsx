import { NextPage } from "next";
import { Subscription } from "src/components/graphql/Subscription";

const SubscriptionPage: NextPage = () => {
  return (
    <div>
      <Subscription />
    </div>
  );
};

export default SubscriptionPage;
